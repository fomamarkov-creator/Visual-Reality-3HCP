name: Compile and Release Visual Reality 3HCP

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-assets:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: visual-reality-3hcp
          - os: windows-latest
            artifact_name: visual-reality-3hcp.exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # Официальный и рабочий метод развертывания экосистемы от Modular
      - name: Set up Magic Build Tool
        uses: prefix-dev/setup-magic@v1
        with:
          run-install: false

      # Скачивание компилятора Mojo 1.0 на основе файла mojoproject.toml
      - name: Install Project Dependencies and Mojo
        run: magic install

      # Сборка исполняемых бинарных файлов для релиза
      - name: Compile Executable Binary
        run: magic run build

      # Загрузка готовых файлов в раздел релизов на GitHub
      - name: Upload Binaries to Release
        uses: softprops/action-gh-release@v2
        if: github.event_name == 'release'
        with:
          files: ${{ matrix.artifact_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # Резервное сохранение файлов в артефакты (при запуске кнопкой в приватном репо)
      - name: Upload Artifacts if Manual Run
        uses: actions/upload-artifact@v4
        if: github.event_name == 'workflow_dispatch'
        with:
          name: ${{ matrix.artifact_name }}-build
          path: ${{ matrix.artifact_name }}
